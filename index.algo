let str,n;
do{
str=prompt("give me the sentences it have to end with .");
n=str.length-1;
}while(str[n]!=".")
let voy_count=0
let word_count=1
let char_count=0
for(let i=0;i<n;i++){
    if("aeyuioAEYUIO".includes(str[i])){voy_count+=1;char_count+=1}
    else {if(str[i]= " "){word_count+=1}
         else {char_count+=1}}
}
alert("voiyelle num"+voy_count+"   words num"+word_count+"  letters num"+char_count)